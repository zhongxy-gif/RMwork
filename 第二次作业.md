# 2.2 第二次作业：理论理解题

## 1. 为什么小陀螺会给传统自瞄算法带来较大挑战？

小陀螺主要改变了装甲板传统的运动轨迹，使其变成高速旋转运动。传统自瞄算法通常通过识别装甲板并结合运动预测进行跟踪，但对于高速旋转的小陀螺，云台的跟踪响应能力会减弱，识别和瞄准容易出现滞后，从而导致击中率大大降低。

此外，装甲板在高速旋转过程中可能快速离开相机视野，使之前预测的目标位置失效，甚至可能造成目标预测错误，因此小陀螺对传统自瞄算法提出了更大的挑战。

## 2. 图像分类和目标检测的核心区别是什么？

图像分类和目标检测的任务不同。

**图像分类（Image Classification）**主要是判断图片或视频中的物体属于什么类别，只需要确定“是什么”。

**目标检测（Object Detection）**不仅需要判断物体的类别，还需要确定物体在图像中的具体位置，通常通过检测框对目标进行定位。

简单来说，**图像分类解决的是“这是什么”，目标检测解决的是“这是什么以及它在哪里”。**

## 3. 什么是相机畸变？为什么不标定会影响距离精度？

相机畸变是由于镜头的光学特性造成的，使相机拍摄到的图像与实际场景之间存在一定偏差。例如，在图像边缘位置可能出现明显的形变。

为了消除这种影响，通常需要通过相机标定求解相机的**内参矩阵和畸变参数**，然后利用这些参数对图像进行畸变校正。

如果不进行相机标定，图像中的位置与实际空间位置之间会一直存在偏差，从而影响目标的**位置定位和距离计算精度**。

## 4. solvePnP 的输入是什么？为什么二维角点误差会导致三维位置抖动？

`solvePnP` 是视觉算法中常用的位姿估计函数。它的主要输入包括：

* 目标物体的三维空间坐标点；
* 对应的二维图像像素坐标点；
* 相机内参矩阵；
* 相机畸变参数。

`solvePnP` 会根据三维点与二维像素点之间的对应关系，反推出目标相对于相机的三维位姿。

由于二维角点存在检测误差，如果输入的二维角点发生微小变化，就可能导致计算得到的**旋转矩阵和平移向量**发生变化。经过位姿解算后，这种误差可能被放大，从而表现为三维位置和姿态的抖动。

因此，**二维角点检测的稳定性会直接影响 solvePnP 最终得到的三维位姿稳定性。**

## 5. 为什么目标运动预测不能只使用相邻两帧的位置差？卡尔曼滤波有什么作用？

不能只使用相邻两帧的位置差来计算目标速度，因为视觉检测本身存在一定的噪声和误差。只使用两帧的位置差，很容易受到单帧检测误差的影响，从而导致计算出的目标速度不准确。

**卡尔曼滤波（Kalman Filter）**是一种结合预测模型和观测数据进行状态估计的方法。它可以根据上一时刻的目标状态预测当前状态，再结合视觉检测得到的实际观测值对预测结果进行修正，从而降低视觉检测噪声对结果的影响。

因此，使用卡尔曼滤波可以使目标的位置和速度估计更加**准确、稳定**，同时为后续的云台控制提供更加可靠的目标数据。
